# DESIGN.md — 技术设计契约

> 本文档是 SPEC.md 的技术实现层翻译。SPEC 说"做什么"，DESIGN 说"精确怎么做"。
> AI Coding 时应严格遵循本文档中的定义，而非自行发明。

## 1. 数据模型（Data Model）

<!-- 每个实体用以下格式定义。字段类型、约束、关系必须精确到可直接生成 DDL/ORM -->

```
Entity: [实体名]
├── [字段名]:    [类型]    ([约束: required/optional/unique/auto/default值])
├── [字段名]:    [类型]    ([约束])
└── 关系: [FK/引用关系说明]

索引:
  - [索引名]: [字段列表] ([类型: unique/composite/partial])
```

<!-- 重复上述模板，直到所有实体定义完毕 -->

## 1.5 共享实体引用（多模块模式）

<!-- 仅在多模块模式下填写。列出本模块引用的 SHARED_SCHEMA 中的共享实体 -->

| 共享实体 | 权威模块 | 本模块使用字段 | 用途 |
|---|---|---|---|
| [实体名] | [权威模块名] | [字段列表] | [在本模块中的用途] |

<!-- 注意：不要重复定义共享实体的字段。引用 SHARED_SCHEMA 中的定义即可。 -->
<!-- 如果本模块需要共享实体的额外字段，应先提议修改 SHARED_SCHEMA，而非自行扩展。 -->

## 2. 枚举与常量（Enums & Constants）

<!-- 每个枚举类型列出完整的值列表。如果有状态机，画出合法转换 -->

```
[枚举名]: value_1 | value_2 | value_3
  - value_1 → value_2: [触发条件]
  - value_2 → value_3: [触发条件]
  - [不可逆转换标注]
```

**系统常量**：

| 常量名 | 值 | 说明 |
|---|---|---|
| [常量名] | [值] | [用途和选择理由] |

## 3. API 契约（API Contract）

<!-- 每个端点完整定义。请求/响应体的每个字段都要有类型。错误码要穷举 -->

```
[METHOD] [PATH]
认证: [需要/不需要, 认证方式]
Request:  { [字段]: [类型] ([约束]) }
Response: { [字段]: [类型] }
Errors:   [HTTP状态码] ([错误码]) — [含义]
副作用:   [如果有, 描述异步操作或事件触发]
```

## 4. 业务规则（Business Rules）

<!-- 用精确的 if-then 语言描述。每条规则必须能直接翻译为代码 -->

**规则 1: [规则名]**
```
WHEN [条件精确描述]
THEN [行为精确描述]
ELSE [否则行为]
```

**规则 2: [规则名]**
```
WHEN ...
THEN ...
```

<!-- 特别关注: 去重逻辑、过期处理、并发冲突、边界条件、权限检查 -->

## 5. 外部依赖与集成（External Dependencies）

<!-- 每个外部服务的调用方式、认证、限制、降级策略 -->

**[服务名]**
- 用途: [在本项目中用来做什么]
- API: [关键调用方法/端点]
- 认证: [认证方式]
- 限制: [已知的速率限制、存储限制、大小限制]
- 降级策略: [服务不可用时怎么办]

---

> **精度检查清单**（DESIGN.md 定稿前，确认以下全部为"是"）：
> - [ ] 所有实体的所有字段都有明确类型和约束？
> - [ ] 所有枚举都列出了完整的值列表？
> - [ ] 所有 API 端点都定义了请求体、响应体和错误码？
> - [ ] 所有业务规则都用 if-then 格式精确描述？
> - [ ] 文档中没有"等"、"类似"、"比如"等模糊词？
> - [ ] 所有外部依赖都标注了已知限制？

---

## 附录：设计决策记录（Design Decision Appendix）

> 本附录由 AI 在 Scaffold+Annotate 定稿后自动生成。
> 记录所有 ✅（无标注）项的选择理由——正文中 ✅ 就是 ✅，扫一眼即可跳过；
> 有经验的用户可翻到此处做一次审计，确认 AI 的默认选择是否合理。

| # | 项 | AI 的选择 | 理由 | 备选方案 |
|---|---|---|---|---|
| 1 | *例：ID 生成策略* | *nanoid 8位* | *短且无序，适合短链场景* | *ULID（可排序）、自增整数（简单但可预测）* |
| 2 | *例：时间存储格式* | *UTC ISO-8601* | *国际化标准，无时区歧义* | *Asia/Shanghai（纯国内项目可考虑）* |
| 3 | *例：密码哈希算法* | *bcrypt cost=12* | *2026 主流推荐，GPU 抗性好* | *argon2id（更强但部署依赖较多）* |
| ... | ... | ... | ... | ... |

<!-- 
使用说明：
- 仅记录 ✅ 项（AI 自信的标准选择），🟡 和 🔴 的决策已在正文中由人类确认
- 每行必须包含"备选方案"列——让审计者知道 AI 考虑过哪些替代项
- 如果用户审计后对某项有异议，将其升级为正文中的 🟡 DECISION 并重新选择
-->
